[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["npm install -g pnpm@latest"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
